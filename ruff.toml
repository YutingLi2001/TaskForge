[lint]
select = ["E", "F", "I"]
ignore = ["E501"]

[lint.per-file-ignores]
# Test files need to set DATABASE_URL before imports
"backend/tests/**/*.py" = ["E402"]

[lint.isort]
known-first-party = ["app"]
